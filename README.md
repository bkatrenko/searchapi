This is simple search API with elasticsearch

to run services just exec [make docker-up]

to test service just exec [make integration-test]

see Makefile for more info

see test.http for example requests